#include<bits/stdc++.h>
using namespace std;

int main()
{
        long long n,p;
        string s;
        scanf("%lld %lld", &n, &p);
        for(int i=0;i<n;i++)
        {
                cin>>s;
        }
	printf("%lld\n", p);
}